The top bar is causing issues on mobile (e.g., overflowing/misaligned). To fix this, let's create a separate top bar for mobile. Let the logo remain as is, that's fine. Instead of a search bar, just a single magnifying glass emoji will be sufficient. Let the 'add channel' button be "+", meaning it should display "+" instead of "addchannel". Let the 3 dots (ellipsis menu) remain as is, but ensure they fit on the screen. Since the logo is already on the left, let these other three elements also be aligned to the right.